class Schroedinger(box: Int) {
    if box > 0 {
       this.living = true;
    } else {
       this.living = false;
    }

    def is_alive() : Boolean {
        return this.living == true;  // this should be ok
    }
}
